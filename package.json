{
  "name": "1000url",
  "version": "1.0.0",
  "private": true,
  "description": "1000url",
  "author": "Jensen ",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "https": "gatsby develop --https --key-file hellfish.test.key --cert-file hellfish.test.crt --ca-file hellfish.test.csr",
    "test": "cd ../gatsby-plugin-advanced-sitemap/ && yarn prepare && cd ../1000url && yarn clean && yarn build && yarn serve"
  },
  "dependencies": {
    "@apollo/client": "^3.6.8",
    "axios": "^0.27.2",
    "gatsby": "^4.14.1",
    "graphql": "^16.5.0",
    "react": "^17.0.1",
    "devcert": "1.2.0",
    "react-dom": "^17.0.1"
  }
}
